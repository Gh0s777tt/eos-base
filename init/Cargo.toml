[package]
name = "init"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
libredox.workspace = true
serde = { version = "1", features = ["derive"] }
serde_json = "1"

config = { path = "../config" }
daemon = { path = "../daemon" }
